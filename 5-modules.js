// in commonjs, every file is module (by default)
//Modules - Encapsulated Code (only share minimum)
const {daniel, blaise}= require('./names')
//const {secret} = require('./names')

//const names = require('./Untitled-1')

const ditBonjour = require('./utilis')



 ditBonjour('daniel')
  ditBonjour(daniel)
 ditBonjour(blaise)
 //ditBonjour(secret)





//cl ditBonjour(names.secret)
 
